export interface ApplicationParametersServerInterface {
  // TODO : For now, no application parameters is sent to the Front, replace "todo: string" by the needed application parameters
  todo: string;
}
